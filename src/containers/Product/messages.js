import { defineMessages } from 'react-intl';

export const scope = 'container.Product';

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: 'Tiết kiệm hàng đầu hôm nay',
  },
});
