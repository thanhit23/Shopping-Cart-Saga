import { defineMessages } from 'react-intl';

export const scope = 'container.Product';
export default defineMessages({
  add_to_cart: {
    id: `${scope}.button_add_cart.product.item`,
    defaultMessage: 'Add To Cart',
  },
});
